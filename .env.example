# Knowledge Service — Example Client Configuration
# ==================================================
# Copy this to .env for local tool usage.
# The .env file is gitignored — do not commit it.
#
# This configures the CLIENT-SIDE tools (health checks, integration
# tests). The SERVER-SIDE config (vLLM endpoints, SeekDB, chunker URL)
# lives in the knowledge-service stack's secrets.env, injected at deploy time.

# Knowledge Service endpoint (deployed on Docker host)
# Replace PORT with the external port mapped to container port 8000
KNOWLEDGE_SERVICE_URL=http://your-docker-host:PORT

# LLM endpoint (optional — used by clean_test_data.py for agentic validation)
# Uncomment and set if you want LLM-assisted test data cleanup.
# VLLM_BASE_URL=http://your-llm-host:PORT/v1
# VLLM_API_KEY=your-api-key
